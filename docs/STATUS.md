# 项目状态

更新日期：2026-09-18。本页汇总代码、2026-09-18 本地检查及截至 2026-09-13 的开发环境实测记录；没有重新检查云端当前状态。

## 当前里程碑

单店预约系统 + 商家工作台 Demo：主要代码已完成，开发环境部署与商家授权已完成，完整验收未完成。

| 模块 | 实现情况 | 验证情况 |
| --- | --- | --- |
| 顾客端 6 页 | 服务、时段选择、结果、个人预约与详情 | 本地测试通过；开发环境有基础预约／取消实测记录 |
| 商家端 4 页 | 工作台、列表、详情、接单日历 | 本地测试通过；云端资格接口与入口已验证 |
| 预约一致性 | 事务、占用检查、幂等、配额、取消 | 内存事务与适配层测试通过，真实云端多实例并发待验收 |
| 关闭／恢复接单 | 显式命令，保留原预约与屏蔽区间 | 本地通过，完整两端联调未完成 |
| 权限 | 微信可信上下文、有效角色及账号映射校验 | 未授权访问实测被拒绝；已配置一个经确认的商家账号 |
| 部署与索引 | booking、operations 已更新；商家日期查询索引已添加 | 2026-09-13 控制台回读确认 |
| 运维 | 日程、清理、核对、预算、备份及发布检查脚本 | 平台调度、告警送达及恢复演练未验收 |

## 本地检查

2026-09-18：三组 TypeScript 类型检查通过，59 项测试全部通过，无失败、跳过或取消。该轮未重新构建；上次完整构建、类型检查及 59 项测试通过的记录见本地测试报告。

对仓库默认生产样例运行发布检查，仍拒绝通过：生产环境、真实门店资料、负责人、资源费用参数和实际验收证据尚未填写。这不表示开发环境已完成的工作被撤销，而是生产验收尚未完成。

## 尚未完成

- 顾客预约 → 商家查看／取消 → 顾客刷新确认的完整流程。
- 关闭日期 → 已有预约保留 → 新预约被拒绝 → 恢复后重新计算可用时段。
- 双账号权限、真实云端并发、断网重试和真机验收。
- 真实门店内容、生产初始化方案、平台计划任务、监控和备份恢复。
- 防爽约与防恶意占位功能，详见路线图。

## 文档约定

设计文档描述目标，不能视为已实现或已验收证明。测试记录保留执行当时的范围，历史记录中的“未部署”等表述只针对当次工作。当前汇总以本页为准。

公开文档不记录真实用户编号、微信身份映射、控制台会话参数或数据库导出。详细私有联调记录由维护者另行保存。
